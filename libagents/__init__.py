"""Libertarian Agents: a shared sandbox for self-determined agents."""

from dotenv import load_dotenv

load_dotenv()  # host-side secrets; never mounted into a sandbox unless asked for

__version__ = "0.1.0"
