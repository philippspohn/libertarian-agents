# Host-side secrets. Never mounted into the sandbox unless listed in an
# environment's `secrets` config.
OPENAI_API_KEY=sk-...
OPENROUTER_API_KEY=sk-or-...

# Where environments and the control-plane DB live (default: ~/.libertarian-agents)
# LIBAGENTS_HOME=/path/to/home
